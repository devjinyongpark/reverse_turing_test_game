# Installation

1. download the project.
2. add `.env` file.
3. write openai api key in the `.env` file.
   ex. `OPENAI_API_KEY = 'sk-...'`
!Note You need openai api key to run the program. You can get it by subscribing opai plus or pro plan.
